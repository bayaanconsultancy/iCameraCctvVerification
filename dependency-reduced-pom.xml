<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.example</groupId>
  <artifactId>iCameraCctvVerification</artifactId>
  <version>1.0</version>
  <description>This project is a tool to verify the configuration of the of CCTVs with
        iCamera.</description>
  <inceptionYear>2025</inceptionYear>
  <organization>
    <name>Consultancy Services</name>
  </organization>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>copy-jre</id>
            <phase>package</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.build.directory}/jre</outputDirectory>
              <resources>
                <resource>
                  <directory>${jre.path}</directory>
                  <targetPath>.</targetPath>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.6.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <finalName>${maven-shaded-jar-name}</finalName>
              <transformers>
                <transformer>
                  <manifestEntries>
                    <Main-Class>${main-class}</Main-Class>
                    <Multi-Release>true</Multi-Release>
                  </manifestEntries>
                </transformer>
                <transformer />
                <transformer />
                <transformer />
                <transformer>
                  <resources>
                    <resource>META-INF/MANIFEST.MF</resource>
                    <resource>META-INF/DEPENDENCIES</resource>
                  </resources>
                </transformer>
              </transformers>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>module-info.class</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
        <dependencies>
          <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-transform-maven-shade-plugin-extensions</artifactId>
            <version>0.2.0</version>
          </dependency>
        </dependencies>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.1.0</version>
        <executions>
          <execution>
            <goals>
              <goal>java</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <mainClass>com.tcs.ion.icamera.cctv.Main</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.akathist.maven.plugins.launch4j</groupId>
        <artifactId>launch4j-maven-plugin</artifactId>
        <version>2.1.2</version>
        <executions>
          <execution>
            <id>l4j-cv</id>
            <phase>package</phase>
            <goals>
              <goal>launch4j</goal>
            </goals>
            <configuration>
              <headerType>gui</headerType>
              <jar>${project.build.directory}/${maven-shaded-jar-name}.jar</jar>
              <outfile>${project.build.directory}/${outfile.name}</outfile>
              <errTitle>${project.name} - Error</errTitle>
              <classPath>
                <mainClass>${main-class}</mainClass>
              </classPath>
              <jre>
                <path>jre</path>
                <opts>
                  <opt>-Djava.net.preferIPv4Stack=true</opt>
                </opts>
              </jre>
              <icon>src/main/resources/logo.ico</icon>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>windows-x64</id>
      <properties>
        <target.os>win</target.os>
        <outfile.name>${project.build.finalName}-${target.arch}.exe</outfile.name>
        <target.arch>x64</target.arch>
        <ffmpeg.classifier>windows-x86_64</ffmpeg.classifier>
        <jre.path>${project.basedir}/jre/${java.version}/${target.arch}/${target.os}</jre.path>
      </properties>
    </profile>
    <profile>
      <id>windows-arm64</id>
      <properties>
        <target.os>win</target.os>
        <outfile.name>${project.build.finalName}-${target.arch}.exe</outfile.name>
        <target.arch>arm64</target.arch>
        <ffmpeg.classifier>windows-x86_64</ffmpeg.classifier>
        <jre.path>${project.basedir}/jre/${java.version}/${target.arch}/${target.os}</jre.path>
      </properties>
    </profile>
    <profile>
      <id>mac-arm64</id>
      <properties>
        <target.os>win</target.os>
        <outfile.name>${project.build.finalName}-${target.arch}.exe</outfile.name>
        <target.arch>arm64</target.arch>
        <ffmpeg.classifier>macosx-arm64</ffmpeg.classifier>
        <jre.path>${project.basedir}/jre/${java.version}/${target.arch}/${target.os}</jre.path>
      </properties>
    </profile>
  </profiles>
  <properties>
    <java.version>21</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <main-class>com.tcs.ion.icamera.cctv.Main</main-class>
    <maven-shaded-jar-name>cv</maven-shaded-jar-name>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>
</project>
